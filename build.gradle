ant.property(environment: "env")

subprojects {
  
  usePlugin "eclipse"

  sourceCompatibility = 1.6
  targetCompatibility = 1.6
  
  repositories { mavenCentral() }
  
}

subprojects.each { it.gwtHome = ant.properties["env.GWT_HOME"] }
